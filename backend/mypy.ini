[mypy]
ignore_missing_imports = True
# SQLAlchemy Column assignments are not resolvable by mypy without sqlalchemy-stubs
# These are false positives — suppress them
disable_error_code = assignment, arg-type, misc, var-annotated
